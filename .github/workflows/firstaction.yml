name: first action
on:
  push:
    branches:
      - main 
jobs:
  first-job:
    runs-on: windows-latest
    steps:
       - run: node --version
       - run: npm --version
       - run: kubectl version